name: Node.js CI

on:
  push:
    branches: ["production", "staging", "feature/add-cicd"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
